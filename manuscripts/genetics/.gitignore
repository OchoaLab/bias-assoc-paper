# local version just adds a few random things
# excluded entirely redundant files, generated by command.bash

# supplement same as main version (from preprint, below this level)
supp.pdf

# figures are just softlinks
figures/
